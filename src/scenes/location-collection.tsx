import * as React from 'react';
import { AppLayout } from 'layouts';
import { LocationCollectionContainer } from 'pods/locations';

export const LocationCollectionScene = () => (
  <AppLayout>
    <LocationCollectionContainer />
  </AppLayout>
);
